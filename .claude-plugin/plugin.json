{
  "name": "sdd",
  "version": "2.2.0",
  "description": "Spec-Driven Development for Claude Code: 22 atomic, stack-agnostic skills + 10 subagents that carry a feature from idea → spec → ux-flows → design → data-model → API → screens → tasks → tests → implement → review → ship, plus a TDD implementation engine and a living decomposition roadmap above the per-feature flow. Start with `survey` (maps an existing codebase or bootstraps an empty one — `scaffold` then materializes the greenfield skeleton), walk the gated backbone — each stage reads the previous artifact and hard-refuses if missing — and keep `docs/roadmap.md` (the idea decomposed into source-anchored steps + a dependency graph + parallel-safe execution waves; never dates) in sync as `specify` marks a step spec'd and `ship` marks it shipped. The Q&A skills are depth-tunable (easy/medium/hard — decide-for-me vs. interrogate-me-with-trade-offs); the ideation analyses run as named subagents (researcher/strategist/analyst/devil's-advocate) gated by that dial; diagrams are presented in prose and written to file (never raw Mermaid dumped to the terminal); and every acceptance criterion is covered end-to-end. design is surface-aware: it declares the feature's target surfaces at the architecture stage (backend-service / web-frontend / mobile-app / desktop-app / cli / worker / library-sdk, each a C4 container) and downstream stages gate their output by that choice — the `ui` task layer, UI-driven sequence flows, and the component / visual-regression / e2e-through-UI frontend test tiers. UI features get a design pipeline (auto-skipped for backend-only work): `design-system` fixes the committed canon (a Figma MCP / Pencil MCP / code-markdown tool choice, platform posture, tokens, component inventory), `ux-flows` derives user flows + the SCR screen inventory, and `screens` writes the per-state screen manifest that tasks/implement/review build against — reuse-first: compose the inventory, a NEW component only with a justification, never a second styling system. Every stage ends with a copy-ready handoff block — what changed, the files to review, then `/clear` + the next `/sdd:…` command.",
  "author": {
    "name": "Kyrylo Genkov",
    "email": "genkovi4@gmail.com",
    "url": "https://github.com/genkovich"
  },
  "license": "MIT",
  "homepage": "https://github.com/genkovich/sdd",
  "repository": "https://github.com/genkovich/sdd.git",
  "keywords": [
    "sdd",
    "spec-driven-development",
    "claude-code",
    "prd",
    "adr",
    "architecture",
    "openapi",
    "tdd"
  ]
}
